# 2026-09-17 — Session 16

## What was done
- Orient: no reply from Josh in daily-in-box. The weekly went out 09-14, so
  none is due tonight (Thursday). The next one is Mon 09-21.
- Live check: all seven pages 200, sitemap/robots 200, `/j` 302 to gates
  (intact), retired URLs 410.
- GSC via the service account: nothing changed. Homepage "Submitted and
  indexed" with its last crawl still 09-11. `/casino-comps/`, `/final-hand/`
  and `/video-poker-trainer/` are still "unknown to Google". Sitemap is still
  pending and has never been downloaded. 90-day totals: homepage 10
  impressions, 1 click, position 2.3. There are also a few impressions on
  dead trails URLs, which are already 410.
- Action: the internal-link gap from last night's plan. No guide linked to
  `/video-poker-trainer/`; only the homepage did. Added three contextual links:
  - casino-comps, slots-vs-VP paragraph: one new sentence saying the 0.46%
    edge only holds with correct play, linking the trainer.
  - casino-comps, "exceptions" paragraph: "full-pay video poker" now links
    to the trainer (it has FPDW at 100.76%).
  - players-cards-compared, Wynn section: "full-pay game" links to the trainer.
  - Bumped dateModified on both guides and their sitemap lastmod to 09-17.
- The trainer already links back to both guides, so the two pages now link
  each other in both directions.
- Deploy: CI success (28 s) on be43839. Checked live with a Googlebot UA:
  both guides are byte-identical to the repo, the links are there (2 and 1),
  the sitemap shows 09-17, `/j` 302, old URLs 410. IndexNow `--all` returned
  200 for 7 URLs. Without `--all` it sent 0, because it was already 09-18 in
  UTC. Same gotcha as before.

## Observed
- Still no crawl of any internal page, 13 days after the valid sitemap.
  Nothing on the site blocks it. What's missing is inbound links, and every
  lever for those is waiting on Josh.

## Thoughts
This was a small change and I meant it that way. It doesn't solve discovery,
because Google has to crawl the guides before these links count. When it
does, the VP trainer, which has the most real user validation of anything
on the site, will have three in-context links instead of one nav card. I
chose not to build another feature for a site nobody can find yet.

## Plans
- Nightly: live + GSC check. The tell is a homepage crawl after 09-11 or
  any internal page moving to "discovered".
- 09-21 weekly: carry the open asks again (Request-indexing clicks, Reddit
  appeal, breakingeven link, blackjacktheforum yes/no). Keep them short and
  ranked. Four asks may be too many, so lead with the two-minute GSC click.
- Candidate small nights: the tournament guide ↔ bet sizer ↔ Final Hand
  links look complete. Check the players-cards page links the comps
  calculator both ways.

## Learning
Internal links cost almost nothing and only pay off after a crawl, so do
them early rather than as a last resort. Run `scripts/indexnow.py` with
`--all` every time, because the evening PDT session is already the next day
in UTC.
