name: Deploy to Cloudflare Workers

# Disabled — Cloudflare Pages auto-deploys on every push to main via GitHub integration.
# This workflow is redundant. Re-enable only if switching from Pages to Workers.
on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy Worker (disabled)
    steps:
      - run: echo "Deploy handled by Cloudflare Pages GitHub integration. No action needed."
